# 디자인 브리프 — 2팀 실시간 배팅 게임

다른 AI/디자이너에게 그대로 전달해서 디자인 시안을 뽑기 위한 문서입니다. 기능은 이미 다 완성되어 있고, **지금은 순수 기본 스타일(흰/검 배경, 기본 버튼)로만 되어 있어 이 위에 시각 디자인을 새로 입히는 작업**이 필요합니다.

## 한 줄 소개

교회 수련회 레크리에이션에서 쓰는 2팀 대항 실시간 베팅 게임. 진행자가 라운드마다 어떤 게임/미션을 진행하고, 두 팀은 그 결과에 자기 팀 포인트를 걸고, 이기면 배팅한 만큼(또는 이벤트에 따라 배수로) 얻고 지면 잃는다.

## 사용 맥락

- 오프라인 현장에서 다같이 진행 — 화면 너머 비대면이 아니라, 같은 공간에 모여서 진행자가 사회를 보며 분위기를 띄우는 상황
- 참가자는 10~20대 위주(청년부/수련회), 밝고 신나는 예능/게임쇼 톤이 어울림
- 승패에 따라 포인트가 오가는 긴장감 + 재미 요소(이벤트)가 핵심 재미

## 화면 4개 (각각 다른 기기)

1. **진행자 화면** — 진행자 1명이 자기 휴대폰으로 조작. 양 팀 점수, 배팅 현황, 라운드 진행 버튼, 이벤트 실행, 결과 판정 등 컨트롤이 다 모여있는 화면. 정보 밀도가 높고 조작이 잦음 — 실수로 잘못 누르지 않게 위험한 동작은 시각적으로 구분되어야 함.
2. **중계 화면** — PC로 열어서 빔프로젝터/TV에 띄우는 화면. 참가자 전원이 이 화면을 보면서 환호하거나 탄식함. **가장 "쇼"다운 연출이 필요한 화면** — 멀리서도 잘 보이는 큰 글자, 점수 변화나 승패가 극적으로 느껴지는 연출.
3. **1팀 화면 / 2팀 화면** — 각 팀 대표 1명이 자기 휴대폰으로 배팅을 입력하는 화면. 구조는 동일(팀 이름/색만 다름). 배팅 입력이라는 "결정의 순간"과, 결과를 기다리는 긴장감, 승패가 갈리는 순간의 임팩트가 핵심.

## 핵심 화면별로 디자인이 필요한 요소

- **현재 보유 포인트**: 모든 화면에서 항상 크게 보여야 하는 가장 중요한 숫자. 마이너스도 자주 나옴(예: `-1,000,000`처럼 매우 큰 마이너스도 가능) — 부호가 잘리거나 레이아웃이 깨지면 안 됨.
- **배팅 입력 화면**: 숫자 키패드 + 빠른 선택 버튼(50P/100P/200P/최대) + 확정 버튼. "얼마를 걸까" 고민하는 순간의 긴장감.
- **배팅 확정 후 대기 화면**: 내가 낸 배팅 금액은 다시 안 보이고, "배팅 완료" 상태 + 상대 팀 대기 상황만 노출. 기다리는 동안의 텐션.
- **승리/패배 결과**: 승리 / 패배 — 이 게임에서 가장 감정적으로 고조되는 순간. 지금은 텍스트+색상뿐이라 여기에 애니메이션/이펙트를 넣고 싶어함(사용자가 우선순위로 꼽은 항목).
- **이벤트 발생**: 두 가지뿐 — "전체 점수 교환"(두 팀 점수가 순간 뒤바뀜), "배팅 배수"(이번 판 승리 포인트가 N배가 됨). 갑자기 튀어나오는 서프라이즈 느낌, 게임쇼의 "이벤트 발생!" 연출과 비슷한 임팩트가 어울림.
- **버튼/카드**: 손가락으로 누르기 쉬운 큰 버튼, 위험한 동작(초기화 등)은 명확히 다른 톤으로 구분.
- **한국어 전용 UI**.

## 데이터/제약 조건

- 팀은 정확히 2개, 팀명은 자유 입력(기본값 "1팀"/"2팀")
- 포인트는 정수, 마이너스 허용, 이론상 매우 큰 수(수백만 단위)까지 가능 — 숫자 자릿수가 늘어나도 안 깨지는 구조 필요
- 라운드는 몇 번이든 반복(정해진 라운드 수 없음)
- 색상은 승리=초록, 패배=빨강, 이벤트=보라 계열을 현재 임시로 쓰고 있음(고정은 아님, 새 디자인에서 바꿔도 됨)

## 지금 상태

기능적으로는 완성(방 생성 → 배팅 → 승패 판정 → 이벤트 → 결과 → 반복 → 최종 순위)되어 있고, Next.js + Tailwind CSS로 구현되어 있어 클래스 기반으로 색/폰트/여백을 새로 입히기 쉬운 구조. 지금 디자인은 다크모드 대응 기본 스타일뿐이라 "이게 게임 같다"는 느낌이 거의 없음 — 이번 리디자인의 목표는 예능/게임쇼 느낌을 살리는 것.
