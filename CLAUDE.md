@AGENTS.md

# 2팀 실시간 배팅 게임 — 교회 수련회용

## 상태

핵심 게임 루프(방 생성 → 팀 링크 접속 → 배팅 라운드 → 승패 판정 → 다음 라운드), 이벤트 시스템(전체 점수 교환 / 배팅 배수 2종류), 되돌리기/기록, 전체 초기화, 최종 순위, 중계(관전자) 화면까지 구현 완료하고 Vercel에 배포함(2026-08-05, `betting-game-olive.vercel.app`). 원래 기획서는 Supabase Auth(익명 로그인) + RLS + SQL RPC 기반의 훨씬 정교한 권한 구조를 요구했지만, 실제로는 로그인이 아예 없는 Prisma 직접 연결 구조(자매 프로젝트 [Borderless](../Borderless)와 동일 패턴)로 전면 전환했다 — 자세한 경위는 [notes/26.08.04.md](notes/26.08.04.md) 참고. 이벤트 종류도 기획서엔 7가지가 있었지만 실제로 필요한 건 2가지뿐이라는 사용자 확인을 받아 대폭 축소했다([notes/26.08.05.md](notes/26.08.05.md) 참고). 진행 상황·의사결정 경위는 [notes/](notes/) 디렉터리에 날짜별로 기록되어 있고, 남은 작업은 [PROGRESS.md](PROGRESS.md) 참고.

**기획서([기획서.txt](기획서.txt))는 이 앱의 출발점이지만 더 이상 정확한 사양이 아니다.** 사용자가 세션 중 여러 항목을 명시적으로 뒤집었으니, 기획서와 아래 "핵심 구조"가 다르면 **아래 내용과 notes/를 우선**할 것.

## 한 줄 요약

진행자 1명(모바일)이 게임방을 만들어 1팀·2팀 대표(모바일)에게 접속 링크를 보내고, 매 라운드 각 팀이 비공개로 포인트를 배팅한 뒤 진행자가 승패를 판정하면 이긴 팀이 포인트를 얻고 진 팀이 잃는 게임. PC로 접속해 TV에 띄우는 중계 화면도 별도로 있음.

## 핵심 구조 (변경 시 기획서보다 이 문서와 notes/를 갱신)

- **로그인/회원가입 없음.** 진행자는 게임방 생성 시 발급되는 `admin_token`이 포함된 비밀 링크(`/admin/[code]/[token]`)로, 각 팀은 팀 전용 토큰 링크(`/play/[code]/[token]`)로 접속한다. 코드를 직접 입력하는 폼은 없음(있었다가 "굳이 필요한가" 피드백으로 제거). 팀 단일기기 강제 접속 기능도 만들었다가 "한 장소에 모여 진행하니 필요 없다"는 이유로 제거함 — 동시에 여러 기기가 같은 팀 링크로 들어가는 걸 막지 않는다.
- **화면은 4개**: 진행자 화면(모바일, `/admin/[code]/[token]`), 중계 화면(PC→TV, `/watch/[code]`, 인증 없이 코드만 알면 조회 가능), 1팀/2팀 화면(모바일, `/play/[code]/[token]`).
- **기술 스택**: Next.js(App Router, TS) + Prisma + `@prisma/adapter-pg` + Supabase Postgres(Vercel 배포). 실시간성은 진짜 push가 아니라 클라이언트 폴링(`PollRefresh`, 1.5~2초 간격)으로 구현.
- **DB 연결은 direct(5432)만 쓰고 pgbouncer 풀링(6543)은 안 쓴다** — 이 Prisma/adapter-pg 조합에서 풀링 연결이 SASL 인증 오류를 내는 버그가 있고, 동시 접속자가 PC 1대+모바일 3대 정도로 적어 풀링이 애초에 불필요함.
- **개발 환경에서 Prisma 클라이언트를 `globalThis`에 캐싱하지 않는다**(`src/lib/prisma.ts`, 프로덕션만 캐싱) — 캐싱하면 스키마를 바꿔도 서버를 완전히 재시작하기 전까진 반영이 안 됨.
- **게임 상태를 보여주는 페이지는 전부 `export const dynamic = "force-dynamic"`을 명시**(`/admin`, `/play`, `/watch`) — Vercel 프로덕션에서 이 설정 없이 캐싱되어 폴링 새로고침이 오래된 화면을 계속 보여주는 버그가 있었음.
- **이벤트는 딱 2종류뿐**: 전체 점수 교환(`SWAP_ALL`), 배팅 배수(`BET_MULTIPLIER`). 둘 다 대상은 항상 양 팀, 공개 방식은 항상 전체 공개 — 대상 팀 선택이나 공개 방식 선택 UI가 없음. **둘 다 라운드가 WAITING(시작 전) 상태일 때만 실행 가능** — BETTING이 시작되면 버튼 자체가 안 보임.
- **배팅 배수 계산은 비대칭**: 이긴 팀만 `배팅액 × 배수`를 얻고, 진 팀은 배수와 무관하게 배팅액만 잃는다(`applyRoundResult`, `ResultForm`).
- **배팅 금액 공개 시점**: 라운드가 끝나기 전(WAITING/BETTING)에는 상대 팀 배팅 금액이 어디에도 노출되면 안 되고, **라운드가 RESOLVED된 뒤에는 전부 공개해도 된다** — 팀 화면에 상대 팀 결과 카드, 중계 화면에 배팅 금액이 결과와 함께 표시됨. 게임 완전 종료 시엔 라운드별 전체 내역표(`RoundHistoryTable`)도 보여줌. 진행자 화면은 예외로, 확정되는 즉시(라운드 중이라도) 금액이 보인다(진행자 개인 기기라 확인 절차가 불필요한 마찰이라는 판단).
- **"진행 중" 배너는 배팅 배수에만 쓴다.** 전체 점수 교환은 실행되는 순간 이미 끝나는 이벤트라 계속 표시하면 안 됨(`getActiveMultiplierEvent` 헬퍼, 진행자/팀/중계 3화면 공통 사용) — 이벤트 실행 자체는 확인창/팝업으로만 1회 안내.
- **전체 초기화 버튼은 진행자 메인 화면이 아니라 "기록/되돌리기" 페이지 안에 있다** — 게임 중 실수로 누를 위험을 줄이기 위해 일부러 덜 눈에 띄는 곳으로 옮김. 이중 확인 + "초기화" 텍스트 직접 입력 필요. 게임 종료 버튼은 파괴적이지 않아 메인 화면에 그대로 있음.
- 점수 변경은 전부 `ScoreTransaction` 원장 테이블에 기록되고, 되돌리기는 값을 직접 되돌리는 게 아니라 보정 트랜잭션을 추가로 쌓는 방식(이후 다른 변경이 있었으면 경고 후 강제 진행 옵션).
- **서버 실행(`npm run dev`)과 `git push`는 절대 대신 실행하지 말 것.** 사용자가 세션 내내 여러 번 명시적으로 요구한 규칙 — 실행 가능한 명령은 코드블록으로만 안내.

## 남은 것

[PROGRESS.md](PROGRESS.md) 참고 — 기획서에서 명시적으로 "가능하다면"/"추가 기능"으로 표시됐던 항목(다크모드, 효과음, 진동, 애니메이션 등) 위주로 2차 범위로 미뤄둔 상태.
