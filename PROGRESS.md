# 진행 상황

기획서([기획서.txt](기획서.txt)) 38개 섹션 기준. ⚠️ 표시는 기획서와 실제 구현이 의도적으로 다른 부분 — 사용자가 세션 중 직접 확인한 변경이니 기획서 말고 이 문서와 [CLAUDE.md](CLAUDE.md)를 따를 것.

## 완료

- **1~3. 개요/권한/접속 방식** — ⚠️ 로그인·비밀번호·코드 입력 없음. 진행자·팀 모두 비밀 토큰이 포함된 링크 클릭만으로 접속(`/admin/[code]/[token]`, `/play/[code]/[token]`). 팀 단일기기 강제(3번, 36-10번)도 ⚠️ 미구현 — 한 장소에 모여 진행하니 불필요하다는 판단.
- **4~5. 기본 게임 설정 / 점수 규칙** — 팀 이름·시작 포인트·마이너스 배팅 한도 설정 가능(방 생성 시). 마이너스 점수 전부 허용, 0으로 보정 안 함.
- **6~12. 배팅 규칙 / 화면 / 상대 팀 비공개** — 배팅 입력·확정·상대 팀 완료 여부 노출까지 구현. ⚠️ 12번(진행자의 "배팅 금액 확인" 버튼)은 제거 — 진행자 화면은 확정 즉시 바로 금액이 보임(진행자 개인 기기라 빔프로젝터 오노출 우려가 없다는 판단, 그 우려는 대신 "중계 화면"을 별도로 분리해서 해소).
- **13~15. 승리·패배 판정/계산/결과 화면** — ⚠️ 13번의 "양 팀 모두 승리/패배" 등 4가지 조합 전부 허용은 미구현. 실제로는 항상 제로섬이라 "어느 팀이 승리했는지" 하나만 고르면 나머지는 자동 패배.
- **16~24. 이벤트 시스템** — ⚠️ 대폭 축소. 기획서의 7종류(점수 지급/차감/이전, 전체 교환, 배수, 강제/지정 추가배팅) 대신 **전체 점수 교환 / 배팅 배수 2종류만** 존재. 공개 방식도 4종(전체공개/대상공개/사실만공개/비밀) 대신 **항상 전체 공개, 항상 양 팀 대상**. 대상 팀 선택 UI, 공개 방식 선택 UI 없음. "직접 이벤트 만들기", "랜덤 이벤트 추첨", "이벤트 사용 횟수 설정"(24번)은 미구현.
- **25~27. 기록 / 되돌리기 / 점수 직접 수정** — `ScoreTransaction` 원장 기반으로 전부 구현. 결과/이벤트/직접수정 각각 되돌리기, 이후 변경 있으면 경고 후 강제 진행 가능.
- **28~29. 실시간 동기화 / 데이터 저장** — ⚠️ 진짜 push가 아니라 폴링(1.5~2초 간격) 기반. 새로고침해도 DB 기준으로 항상 복구됨(서버 상태가 유일한 진실).
- **30. 관전자 화면** — "중계 화면"이라는 이름으로 구현(`/watch/[code]`). ⚠️ 사용 여부를 진행자가 켜고 끄는 토글은 미구현 — 항상 켜져 있고, 코드만 알면 누구나 볼 수 있음(인증 없음, 민감 정보는 애초에 안 보여주므로 문제 없다고 판단).
- **31. 최종 순위** — 마이너스 포함 실제 크기 비교, 동점 시 "공동 우승" 표시. 연장전 옵션은 없음(공동 우승으로 통일).
- **32. 입력값 검증** — 배팅 금액 등 클라이언트+서버 양쪽 검증.
- **33. UI/디자인** — 한국어, 큰 버튼, 마이너스/큰 숫자 레이아웃 깨짐 없음. 게임쇼풍 디자인은 기본적인 수준(색상 강조, 이모지)만 적용.
- **35. 전체 초기화** — 이중 확인 + "초기화" 텍스트 직접 입력. ⚠️ 위치를 진행자 메인 화면이 아니라 "기록/되돌리기" 페이지로 옮김(오조작 방지).
- **36. 기술적 사항** — ⚠️ "진행자/팀 권한을 서버 수준에서 구분"은 RLS 대신 각 Server Action이 `admin_token`/`accessToken`을 직접 검증하는 방식으로 동일한 효과를 달성. 결과/이벤트 중복 적용 방지, 트랜잭션 기반 점수 변경 등은 구현.

## 확인 후 안 만들기로 결론남

기획서(`기획서.txt`)는 사용자가 직접 쓴 게 아니라 AI가 만든 초안이라, 세션 중 하나씩 필요성을 다시 확인했고 아래는 전부 "실제로는 불필요"로 결론남 — TODO 아님, 더 이상 만들 계획 없음.

- **게임 도중 설정 변경**(팀 이름/시작 포인트/마이너스 배팅 한도, 2·4번) — 방 생성 시에만 정하고 이후엔 못 바꿈
- **라운드 임의 이동**(2번 "현재 라운드 변경") — "다음 라운드"로 1씩만 증가
- 34번: 다크모드 수동 토글(시스템 설정 따라 자동 전환은 이미 됨), 효과음/진동 ON-OFF, 이벤트·승리·패배 애니메이션, 접속 상태 표시, 마지막 동기화 시간 표시
- 24번: 직접 이벤트 만들기, 랜덤 이벤트 추첨, 이벤트 사용 횟수 설정
- 30번: 관전자 화면(중계 화면) 사용 여부 토글 — 관련 필드(`spectatorEnabled`, `soundEnabled`, `vibrationEnabled`)는 스키마에서도 제거함(2026-08-05)

## 참고

- 왜 이렇게 됐는지 경위는 [notes/26.08.04.md](notes/26.08.04.md), [notes/26.08.05.md](notes/26.08.05.md) 참고
- 배포: `betting-game-olive.vercel.app` (Vercel, GitHub `doxazo-JY/betting-game` 연동)
